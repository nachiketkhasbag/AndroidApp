<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cooker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cooker_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cooker_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:minSdkVersion="8">

        <!-- TODO: Need to change this -->
        <!-- [START fcm_default_icon] -->
        <!-- Set custom default icon. This is used when no icon is set for incoming notification messages.
             See README(https://goo.gl/l4GJaQ) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_menu_gallery" />
        <!-- Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
        <!-- [END fcm_default_icon] -->

        <!-- [START fcm_default_channel] -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id"/>
        <!-- [END fcm_default_channel] -->

        <!-- Splash screen -->
        <activity
            android:name=".Common.MainActivity"
            android:theme="@style/CookerAppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- [START firebase_iid_service] -->
        <service android:name=".Common.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <!-- [START AppFireBaseMessagingService] -->
        <service android:name=".Common.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".Common.SignupActivity"
            android:theme="@style/CookerAppThemeEntryPoint" />
        <activity
            android:name=".Common.ForgotPassword"
            android:theme="@style/CookerAppThemeEntryPoint" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/fb_app_id" />

        <activity android:name=".Common.WelcomeActivity" />
        <activity
            android:name=".GuestActivityFragments.GuestActivity"
            android:label="GuestActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ChefActivityFragments.ChefActivity"
            android:label="@string/title_activity_chef"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Common.UserInformation"
            android:label="UserInformation"
            android:theme="@style/CookerAppThemeEntryPoint" />
    </application>

</manifest>